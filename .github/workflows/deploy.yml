name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Scala CLI
        uses: VirtusLab/scala-cli-setup@v1
        with:
          jvm: temurin:21
      - name: Cache Coursier
        uses: actions/cache@v6
        with:
          path: ~/.cache/coursier
          key: coursier-${{ hashFiles('snippets/*.scala') }}
          restore-keys: coursier-
      - name: Compile snippets
        run: ./scripts/compile-snippets.sh
      - name: Install, build, and upload site
        uses: withastro/action@v6
        with:
          node-version: 22
        # Lets the build bake the star counts into the cards; without it they are fetched by the
        # browser only, against a rate limit shared by everyone on the reader's IP.
        env:
          GITHUB_TOKEN: ${{ github.token }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    # Only the deploy job needs write access to Pages and an OIDC token.
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
